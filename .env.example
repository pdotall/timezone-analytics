# Cloudflare Worker that forwards to api.sim.dune.com and adds SIM_API_KEY
SIM_PROXY_URL=https://smart-money.pdotcapital.workers.dev/v1


# Optional: per-chain activity fetch limit
SIM_ACTIVITY_LIMIT=1000

# Dev server port
PORT=3001
